apiVersion: k8s.keycloak.org/v2alpha1
kind: Keycloak
metadata:
  name: keycloak
spec:
  instances: 1

  bootstrapAdmin:
    user:
      secret: keycloak-admin

  http:
    httpEnabled: true

  hostname:
    strict: false