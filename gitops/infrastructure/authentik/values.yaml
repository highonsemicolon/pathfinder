authentik:
  secret_key: "f2IEP6FTBYD+uMJdR2q5qx1IwjoXpISOGDQYwau0Q/IzSoeeZuE9/OJ5/vljgHi8PDpbEx+5ZngDdDTy"

  error_reporting:
    enabled: false

  postgresql:
    password: "file:///postgres-creds/password"

postgresql:
  enabled: true
  auth:
    existingSecret: authentik-postgres

server:
  volumes:
    - name: postgres-creds
      secret:
        secretName: authentik-postgres

  volumeMounts:
    - name: postgres-creds
      mountPath: /postgres-creds
      readOnly: true

worker:
  volumes:
    - name: postgres-creds
      secret:
        secretName: authentik-postgres

  volumeMounts:
    - name: postgres-creds
      mountPath: /postgres-creds
      readOnly: true