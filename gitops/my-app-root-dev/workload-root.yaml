apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: workload-root
  namespace: argocd
spec:
  generators:
    - matrix:
        generators:
          - clusters:
              selector:
                matchLabels:
                  application: my-app
          - git:
              repoURL: https://github.com/highonsemicolon/pathfinder.git
              revision: main
              directories:
                - path: gitops/applications/my-app/{{metadata.labels.environment}}/services/*/*
  template:
    metadata:
      name: '{{metadata.labels.application}}-{{path[6]}}-{{metadata.labels.environment}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/highonsemicolon/pathfinder.git
        targetRevision: main
        path: helm
        helm:
          valueFiles:
            - /{{path}}/values.yaml
      destination:
        server: '{{server}}'
        namespace: '{{path[5]}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
