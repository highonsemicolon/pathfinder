{{- if .Values.volume.configMap }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pathfinder.fullname" . }}-volume
  labels:
    {{- include "pathfinder.labels" . | nindent 4 }}
data:
  {{- range $key, $_ := .Values.volume.configMap }}
  {{ $key }}: |
    {{- $.Files.Get (printf "files/%s" $key) | nindent 4 }}
  {{- end }}
{{- end }}